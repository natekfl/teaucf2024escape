plugins {
    alias(libs.plugins.jvm)
    id 'application'
    id 'org.openjfx.javafxplugin' version '0.1.0'
}

repositories {
    mavenCentral()
}

dependencies {

}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(17)
    }
}

application {
    mainClass = 'info.nathankutzan.projectors.AppKt'
}
javafx {
    version = "21.0.2"
    modules = [ 'javafx.controls', 'javafx.media' ]
}

